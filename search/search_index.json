{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home","text":""},{"location":"#welcome-to-streamlit-chat-st-chat","title":"Welcome to Streamlit Chat [st-chat]","text":"<p>Streamlit Component, for a Chat-bot UI, example app</p> <p>authors - @yashppawar &amp; @YashVardhan-AI</p>"},{"location":"#installation","title":"Installation","text":"<p>Install <code>streamlit-chat</code> with pip</p> <pre><code>pip install streamlit-chat \n</code></pre> <p>usage, import the <code>message</code> function from <code>streamlit_chat</code></p> <pre><code>import streamlit as st\nfrom streamlit_chat import message\n\nmessage(\"My message\") \nmessage(\"Hello bot!\", is_user=True)  # align's the message to the right\n</code></pre>"},{"location":"#screenshot","title":"Screenshot","text":""},{"location":"contributing/","title":"Contributing","text":"<p>npm i react-markdown rehype-highlight rehype-katex rehype-raw remark-gfm remark-math</p>"},{"location":"creating-streamlit-component/","title":"Creating a Streamlit Component","text":"<p>using react</p>"},{"location":"creating-streamlit-component/#initiate-the-frontend","title":"initiate the frontend","text":"<p>we will be using vite for initiating the frontend, as its quite fast compared to create react app^1</p> <pre><code>$ npm create vite@latest\n</code></pre> <p>give the project name as <code>frontend</code>, select the framework as react (or any other of your choice)</p> <pre><code>? Select a framework: \u203a - Use arrow-keys. Return to submit.\n    Vanilla\n    Vue\n\u276f   React\n    Preact\n    Lit\n    Svelte\n    Others\n</code></pre> <p>depending on the language you want to use, choose that language variant</p> <pre><code>? Select a variant: \u203a - Use arrow-keys. Return to submit.\n    TypeScript\n\u276f   TypeScript + SWC\n    JavaScript\n    JavaScript + SWC\n</code></pre>"},{"location":"creating-streamlit-component/#installing-necessary-packages","title":"installing necessary packages","text":"<p>now <code>cd</code> into the project directory <code>cd frontend</code> and execute </p> <pre><code>$ npm install\n</code></pre> <p>after that, install the <code>streamlit-component-lib</code></p> <pre><code>$ npm install streamlit-component-lib\n</code></pre> <p>after executing these commands, edit the :file: <code>vite.config.ts</code> to look like</p> <pre><code>import { defineConfig } from 'vite'\nimport react from '@vitejs/plugin-react-swc'\n\n// https://vitejs.dev/config/\nexport default defineConfig({\n  plugins: [react()],\n  server: {\n    port: 3001,  // to launch the server on port 3001\n  },\n  base: '',      // to use absolute url's in the build, might not work without it\n})\n</code></pre> <p>now using this setup, create your component, to run the development server run the following command</p> <pre><code>$ npm run dev\n</code></pre> <p>to build your component run</p> <pre><code>$ npm run build\n</code></pre> <p>now build the wheel</p> <pre><code>$ python setup.py sdist bdist_wheel\n</code></pre> <p>and at last upload it to pypi using twine (testpypi)</p> <pre><code>$ python3 -m twine upload --repository testpypi dist/*\n</code></pre> <p>(pypi)</p> <pre><code>$ python3 -m twine upload dist/*\n</code></pre>"}]}