import React from 'react'
import ReactDOM from 'react-dom/client'
import Chat from './stChat.tsx'
// import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <Chat />
  </React.StrictMode>,
)
